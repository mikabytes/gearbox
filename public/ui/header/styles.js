import { css } from "../../component"

export default css`
  :host {
    background-color: var(--lightgray);
    display: flex;
    align-items: center;
    padding: 0 10px;
    border-bottom: 1px solid var(--gray);
  }
`
